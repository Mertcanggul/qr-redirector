<%- include('layout', { title: 'Redirect Page' }) %>
<script>
  // Yönlendirilecek URL'yi dinamik olarak almak
  const redirectUrl = "<%= url %>";
  // Sayfa yüklenir yüklenmez yönlendirme
  window.location.href = redirectUrl;
</script>
